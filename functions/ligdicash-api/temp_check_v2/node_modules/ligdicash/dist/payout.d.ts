import { HTTPProvider } from "./providers";
import { ApiConfig } from "./types/globals";
import { BaseResponseType } from "./types/responses";
export type WithdrawalSendParam = ({
    type: "client";
} & {
    to_wallet?: boolean;
    callback_url?: string;
    custom_data?: Record<string, any>;
}) | ({
    type: "merchant";
} & {
    callback_url?: string;
    custom_data?: Record<string, any>;
});
export declare class Withdrawal extends HTTPProvider {
    amount: number;
    description: string;
    customer: string;
    constructor(config: ApiConfig, amount: number, description: string, customer: string);
    send(params: WithdrawalSendParam): Promise<BaseResponseType>;
}
