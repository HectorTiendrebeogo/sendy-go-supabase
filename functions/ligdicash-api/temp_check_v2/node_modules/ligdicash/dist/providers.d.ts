import { ApiConfig, FeatureType, PlatformType } from "./types/globals";
import { InitialLigdicashResponseType, LigdicashResponseType } from "./types/responses";
export declare abstract class Base {
    private apiKey;
    private authToken;
    baseUrl: string;
    platform: PlatformType;
    constructor(config?: ApiConfig);
    buildUrl(url: string): string;
    protected request<T>(endpoint: string, options?: RequestInit): Promise<T>;
}
export declare class HTTPProvider extends Base {
    getDataOrRaiseError(response: InitialLigdicashResponseType, feature: FeatureType): Promise<LigdicashResponseType>;
    get<T>(url: string, feature: FeatureType): Promise<T>;
    post<T>(url: string, payload: Object, feature: FeatureType): Promise<T>;
}
