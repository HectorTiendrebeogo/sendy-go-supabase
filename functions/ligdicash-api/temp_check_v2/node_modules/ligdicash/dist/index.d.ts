import { Invoice as Invoice_, InvoiceParam } from "./payin";
import { ApiConfig, PlatformType } from "./types/globals";
import { Withdrawal as Withdrawal_ } from "./payout";
export default class Ligdicash {
    apiKey: string;
    authToken: string;
    baseUrl: string;
    platform: PlatformType;
    constructor(config: ApiConfig);
    Withdrawal(amount: number, description: string, customer: string): Withdrawal_;
    Invoice(invoice: InvoiceParam): Invoice_;
    getTransaction(token: string, type?: "payin" | "client_payout" | "merchant_payout"): Promise<import("./types/responses").StatusResponseType>;
}
